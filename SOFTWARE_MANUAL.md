# 小野兔 Rabbit 软件功能说明书

> 适用版本：v0.6.0
> 更新日期：2026-07-29
> 项目地址：https://github.com/benbenzhuyi/rabbit-editor

## 1. 软件简介

小野兔 Rabbit 是一款轻量级、开源、本地优先的 AI 辅助 Markdown 文档编辑器。软件以长文写作和结构化编辑为核心，将 Markdown 源码编辑、实时预览、文件管理、大纲导航、AI 对话和选区级 AI 改写整合在同一个桌面工作台中。

主要特点：

- Electron 桌面应用，支持本地文件直接读写。
- CodeMirror 6 Markdown 编辑器。
- Markdown 源码模式与渲染预览模式。
- 本地 llama.cpp、Ollama 以及 OpenAI 兼容 API。
- Ctrl+K 选区级 AI 润色、续写、翻译和自定义处理。
- Ctrl+L 将编辑器选区或当前行引用到 AI 对话。
- 文件浏览器、大纲导航和文档级 AI 对话。
- 深色、浅色主题与三种窗口模式。
- 十种界面语言。
- MIT 开源许可。

## 2. 界面组成

主界面由以下区域组成：

1. **菜单栏**：文件、编辑、视图。
2. **左边栏**：文件浏览器和大纲导航。
3. **中央编辑区**：Markdown 源码编辑或渲染预览。
4. **右边栏**：AI 对话、模型选择和消息操作。
5. **状态栏**：光标位置、字数、选中字数、保存状态、AI 状态、模型、温度、换行状态、源码/预览模式和设置入口。

左右侧边栏可以拖动改变宽度；左侧的文件浏览器和大纲导航之间可以拖动分隔线调整高度。

## 3. 菜单功能

### 3.1 文件菜单

| 菜单项 | 功能 | 快捷键 |
|---|---|---|
| 新建 | 新建空白文档 | Ctrl+N |
| 打开 | 打开本地文本文件 | Ctrl+O |
| 保存 | 保存当前文档 | Ctrl+S |
| 另存为 | 将当前文档保存到新位置 | Ctrl+Shift+S |
| 打开文件夹 | 将文件夹载入左侧文件浏览器 | Ctrl+Shift+O |
| 最近文件 | 显示最近打开的文件，最多 5 个 | — |
| 关闭文件 | 关闭当前文档 | Ctrl+W |
| 退出 | 退出小野兔 | — |

存在未保存修改时，新建、打开、关闭或退出会先显示保存确认。

### 3.2 编辑菜单

| 菜单项 | 功能 | 快捷键 |
|---|---|---|
| 撤销 | 撤销上一步编辑 | Ctrl+Z |
| 重做 | 恢复被撤销的编辑 | Ctrl+Y |
| 剪切 | 剪切选中内容 | Ctrl+X |
| 复制 | 复制选中内容 | Ctrl+C |
| 粘贴 | 粘贴剪贴板内容 | Ctrl+V |
| 全选 | 选择全文 | Ctrl+A |
| 复制行 | 复制光标所在行 | Ctrl+D |
| 删除行 | 删除光标所在行 | Ctrl+Shift+K |
| 选区弹窗编辑 | 打开 Ctrl+K 选区 AI 处理窗口 | Ctrl+K |
| 选区引用AI | 将选区或当前行引用到 AI 对话 | Ctrl+L |
| AI回复替换选区 | 用最近一次 AI 回复替换当前选区 | Ctrl+Shift+T |
| AI回复插入选区 | 将最近一次 AI 回复插入当前选区后方 | Ctrl+Shift+I |
| 复制AI回复 | 复制最近一次 AI 回复 | Ctrl+Shift+C |
| 设置 | 打开 AI 与程序设置窗口 | Ctrl+, |

### 3.3 视图菜单

| 菜单项 | 功能 | 快捷键 |
|---|---|---|
| 预览切换 | 在 Markdown 源码和渲染预览之间切换 | Ctrl+Shift+P |
| 自动换行 | 开启或关闭源码编辑器自动换行 | Ctrl+Shift+W |
| 放大 | 放大源码和预览内容 | Ctrl+= |
| 缩小 | 缩小源码和预览内容 | Ctrl+- |
| 重置缩放 | 恢复默认字号 | Ctrl+0 |
| 左边栏 | 显示或隐藏左边栏 | Ctrl+B |
| 右边栏 | 显示或隐藏右侧 AI 面板 | Ctrl+J |
| 深色主题 | 使用深色界面 | Ctrl+Alt+T |
| 浅色主题 | 使用浅色界面 | Ctrl+Alt+T |
| 正常窗口 | 切换到标准窗口模式 | Ctrl+Shift+1 |
| 全屏有菜单 | 全屏显示并保留菜单栏 | Ctrl+Shift+2 |
| 全屏无菜单 | 全屏显示并隐藏菜单栏 | Ctrl+Shift+3 |
| 窗口模式轮换 | 在三种窗口模式之间循环 | F11 |
| 退出保存窗口 | 保存并在下次启动时恢复窗口布局状态 | Ctrl+Alt+R |
| 语言 | 选择界面语言，重启后生效 | — |

菜单中的勾选标志表示当前启用状态，包括自动换行、左右边栏、主题、退出保存窗口和当前语言。

## 4. Markdown 编辑功能

### 4.1 源码编辑

- Markdown 语法高亮。
- H1—H6 标题分级着色。
- 行号显示。
- 当前行高亮。
- 自动括号和常用编辑行为。
- 撤销、重做、复制、粘贴和全选。
- 复制当前行、删除当前行。
- 将当前行向上或向下移动。
- 可切换自动换行。
- 编辑内容变化后更新标题栏、保存状态、字数和大纲。

### 4.2 预览模式

- 使用 marked 渲染 Markdown。
- 使用 DOMPurify 清理渲染结果。
- 支持标题、段落、列表、引用、链接、表格、图片、分隔线、任务列表和代码块。
- 代码块使用 highlight.js 进行语法高亮。
- 源码和预览切换时尽量保持相同阅读位置。
- 源码和预览共享同一缩放级别。
- 预览模式支持查找和匹配高亮。
- 点击大纲标题可滚动到对应预览位置。

### 4.3 缩放

缩放同时作用于源码编辑器和 Markdown 预览：

- Ctrl+=：放大。
- Ctrl+-：缩小。
- Ctrl+0：恢复默认字号。
- Ctrl+鼠标滚轮：连续放大或缩小。

切换源码与预览模式后会保留当前缩放级别。

## 5. 文件与工作区

### 5.1 支持打开的文本格式

Markdown、TXT、HTML、HTM、JSON、JavaScript、CSS、XML、YAML、YML、CSV、LOG、RST、TeX、Python、Java、C、C++、头文件和 Shell 脚本。

“另存为”默认保存为 Markdown 文件。

### 5.2 文件浏览器

- 默认显示系统文档目录。
- 打开文件夹后将其作为当前工作区。
- 文件夹按目录优先、名称排序。
- 单击文件可打开；Markdown 文件从文件浏览器打开时进入预览模式。
- 单击文件夹可展开或折叠。
- Shift+单击文件夹箭头可展开其子目录。
- 顶部按钮可刷新目录或全部折叠。
- `..` 可返回上一级目录。
- 双击文件或文件夹名称可以重命名。
- 右键菜单支持新建文件、新建文件夹、重命名和删除。
- 当前文件在树中保持选中高亮。

### 5.3 拖放与最近文件

- 可以将支持的文本文件拖入窗口打开。
- 最近文件菜单最多保存 5 条记录。
- 文件打开或保存后会更新最近文件列表。

### 5.4 数据安全

- 覆盖已有文件前自动创建 `.bak.md` 备份。
- 为防止数据丢失，不允许用意外产生的空内容覆盖已有非空文件。
- 未保存修改在关闭、切换文件或退出时会触发确认。

## 6. 大纲导航

- 自动解析 Markdown H1—H6 标题。
- 不同层级使用缩进和颜色区分。
- 点击标题跳转到源码行或预览标题。
- 标题层级可以折叠。
- 支持按指定标题级别批量折叠。
- 支持一键展开全部标题。
- 编辑文档后大纲自动刷新。

## 7. AI 辅助功能

### 7.1 模型接入

软件调用 OpenAI 兼容的 `/chat/completions` 接口，可用于：

- 本地 llama.cpp 服务。
- 本地 Ollama OpenAI 兼容接口。
- OpenAI 兼容在线服务。
- DeepSeek。
- Claude 兼容服务。
- 用户自行部署的兼容 API。

可配置项目：

- 基础地址。
- API 密钥。
- 模型名称。
- 默认 AI 模式。
- Ctrl+K 默认字数。
- 最大输出 Tokens。
- 温度值。
- 每种模式的自定义系统提示词。

预置模型名称包括 `deepseek-chat`、`deepseek-reasoner`、`gpt-4o`、`gpt-4o-mini`、`claude-sonnet-4-20250514` 和 `local-model`，也可以输入配置中的自定义模型名称。

### 7.2 Ctrl+K 选区处理

在编辑区选择文字后按 Ctrl+K，可以执行：

- 润色。
- 续写。
- 定制处理。
- 英译中。
- 中译英。

还可以设置目标字数和额外指令。没有选区时，软件自动选中当前行。

执行规则：

- 润色、翻译和定制模式会替换选中内容。
- 续写模式会把生成结果插入到选区后方。
- Ctrl+Enter 确认执行。
- Esc 关闭浮动窗口。

### 7.3 Ctrl+L 引用到 AI

- 有选区时，引用选中文字及其行号。
- 没有选区时，引用当前行并自动选中该行。
- 引用以 `@文件名 起始行-结束行` 的形式显示在 AI 输入框。
- 实际引用正文随请求发送，并在对话消息中折叠显示。
- 若右边栏已隐藏，会自动打开右边栏并聚焦输入框。

### 7.4 AI 对话

- 输入自然语言写作要求并发送。
- Enter 发送，Shift+Enter 换行。
- 支持新建对话。
- 对话记录按当前文件分别保存。
- 切换文件时自动加载该文件的历史对话。
- 用户消息支持复制、编辑、删除和重新提交。
- AI 消息支持复制、替换编辑器选区、插入到选区后方和删除。
- 编辑或重新提交历史消息时，会从该消息位置重新生成后续对话。
- 支持快速复制、替换或插入最近一次 AI 回复。

## 8. 查找与替换

- Ctrl+F 打开查找。
- Ctrl+H 打开查找替换。
- 有选区时自动使用选中文字作为查找关键词。
- 查找窗口可以拖动。
- 输入关键词后高亮全部匹配，并突出显示当前匹配。
- 显示“当前序号 / 总匹配数”。
- Enter 跳到下一个匹配。
- Shift+Enter 跳到上一个匹配。
- 支持替换当前匹配和全部替换。
- Esc 关闭查找窗口并清除高亮。
- 源码被编辑后会重新计算匹配范围，避免高亮位置漂移。
- 查找功能同时支持源码模式和预览模式。

## 9. 主题、侧栏与窗口

### 9.1 主题

- 深色主题。
- 浅色主题。
- Ctrl+Alt+T 在两种主题之间切换。
- 菜单勾选显示当前主题。

### 9.2 侧边栏

- Ctrl+B 显示或隐藏左边栏。
- Ctrl+J 显示或隐藏右边栏。
- 菜单勾选与实际显示状态同步。
- Alt+L 会自动显示右边栏并聚焦 AI 输入框。

### 9.3 窗口模式

| 模式 | 快捷键 | 说明 |
|---|---|---|
| 普通窗口 | Ctrl+Shift+1 | 标准窗口模式 |
| 全屏有菜单 | Ctrl+Shift+2 | 全屏显示，保留菜单栏 |
| 全屏无菜单 | Ctrl+Shift+3 | 全屏并隐藏菜单栏 |
| 循环切换 | F11 | 在三种模式之间循环 |

启用“退出保存窗口”后，软件会保存窗口位置、尺寸、窗口模式和左右侧边栏显示状态，并在下次启动时恢复。

## 10. 多语言界面

“视图 → 语言”提供以下语言：

| 语言 | 图标 |
|---|---|
| 中文（默认） | 中国国旗 |
| English | 英国国旗 |
| Français | 法国国旗 |
| Русский | 俄罗斯国旗 |
| Español | 西班牙国旗 |
| Português | 葡萄牙国旗 |
| Deutsch | 德国国旗 |
| Italiano | 意大利国旗 |
| 日本語 | 日本国旗 |
| 조선말 | 朝鲜国旗 |

选择语言后，设置会立即保存；重新启动小野兔后，菜单、侧边栏、AI 面板、设置窗口和状态栏等主要界面将使用所选语言。

## 11. 设置与数据存储

### 11.1 设置入口

- 编辑 → 设置。
- 状态栏右侧“设置”。
- Ctrl+,。

### 11.2 本地数据

用户数据默认存储在 Electron 的 `userData` 目录中：

- `settings.json`：AI 参数、语言、窗口恢复等设置。
- `recent_files.json`：最近文件。
- `window-state.json`：窗口位置、尺寸、模式和侧栏状态。
- `conversations/`：按文件路径生成独立的 AI 对话记录。

API 密钥和对话记录保存在本机，不需要云端账户。

## 12. 快捷键完整清单

### 12.1 文件操作

| 快捷键 | 功能 |
|---|---|
| Ctrl+N | 新建文件 |
| Ctrl+O | 打开文件 |
| Ctrl+Shift+O | 打开文件夹 |
| Ctrl+S | 保存 |
| Ctrl+Shift+S | 另存为 |
| Ctrl+W | 关闭当前文件 |

### 12.2 编辑操作

| 快捷键 | 功能 |
|---|---|
| Ctrl+Z | 撤销 |
| Ctrl+Y | 重做 |
| Ctrl+X | 剪切 |
| Ctrl+C | 复制 |
| Ctrl+V | 粘贴 |
| Ctrl+A | 全选 |
| Ctrl+D | 复制当前行 |
| Ctrl+Shift+K | 删除当前行 |
| Alt+↑ | 当前行上移 |
| Alt+↓ | 当前行下移 |

### 12.3 AI 辅助

| 快捷键 | 功能 |
|---|---|
| Ctrl+K | 打开选区 AI 处理窗口 |
| Ctrl+Enter | 在 Ctrl+K 窗口中确认执行 |
| Ctrl+L | 将选区或当前行引用到 AI |
| Alt+L | 聚焦 AI 输入框 |
| Ctrl+Shift+C | 复制最近一次 AI 回复 |
| Ctrl+Shift+T | 用最近一次 AI 回复替换选区 |
| Ctrl+Shift+I | 将最近一次 AI 回复插入选区后方 |
| Enter | 在 AI 输入框中发送 |
| Shift+Enter | 在 AI 输入框中换行 |
| Esc | 取消消息编辑、关闭 Ctrl+K 或移出 AI 输入框焦点 |

### 12.4 查找替换

| 快捷键 | 功能 |
|---|---|
| Ctrl+F | 打开查找 |
| Ctrl+H | 打开替换 |
| Enter | 下一个匹配 |
| Shift+Enter | 上一个匹配 |
| Esc | 关闭查找替换并清除高亮 |

### 12.5 视图与布局

| 快捷键 | 功能 |
|---|---|
| Ctrl+Shift+P | 切换源码/预览 |
| Ctrl+Shift+W | 切换自动换行 |
| Ctrl+= | 放大源码和预览 |
| Ctrl+- | 缩小源码和预览 |
| Ctrl+0 | 重置缩放 |
| Ctrl+鼠标滚轮 | 连续缩放源码和预览 |
| Ctrl+B | 显示/隐藏左边栏 |
| Ctrl+J | 显示/隐藏右边栏 |
| Ctrl+Alt+T | 切换深色/浅色主题 |
| Ctrl+Alt+R | 切换“退出保存窗口” |
| Ctrl+Shift+1 | 普通窗口 |
| Ctrl+Shift+2 | 全屏保留菜单 |
| Ctrl+Shift+3 | 沉浸全屏 |
| F11 | 循环切换窗口模式 |

### 12.6 大纲导航

| 快捷键 | 功能 |
|---|---|
| Alt+Shift+1～6 | 折叠到指定标题级别 |
| Alt+Shift+9 | 展开全部标题 |

### 12.7 菜单与设置

| 快捷键 | 功能 |
|---|---|
| Alt+F | 打开文件菜单 |
| Alt+E | 打开编辑菜单 |
| Alt+V | 打开视图菜单 |
| Ctrl+, | 打开设置 |

## 13. 状态栏说明

状态栏从左到右显示：

- 当前光标行、列。
- 全文字数。
- 当前选区字数。
- 保存或未保存状态。
- AI 是否正在生成。
- 当前模型。
- 温度值；可单击直接修改。
- 自动换行状态；可单击切换。
- 设置入口。
- 源码/预览模式；可单击切换。

## 14. 运行与发布

开发运行：

```bash
npm install
npm start
```

只构建渲染进程：

```bash
npm run build:renderer
```

运行自动化测试：

```bash
npm test
```

打包 Windows 版本：

```bash
npm run pack
```

## 15. 已知边界

- 语言切换需要重启软件。
- 软件要求 AI 服务提供 OpenAI 兼容接口。
- 预览模式中的替换操作仍以源码编辑器内容为最终修改对象。
- F1 帮助入口目前保留但尚未提供独立帮助窗口。
- 当前自动化测试覆盖核心行编辑命令；界面菜单和跨模式交互仍应在发布前进行人工回归。
